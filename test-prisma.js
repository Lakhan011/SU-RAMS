const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const scholar = await prisma.scholar.findFirst({ include: { school: true, department: true, verifications: true, supervisor: true, courses: { include: { course: true } } } }); console.log('Scholar found:', scholar ? scholar.id : 'None'); } catch (e) { console.error('PRISMA ERROR:', e); } finally { await prisma.$disconnect(); } } main();