# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

DATABASE_URL="mysql://root:@127.0.0.1:3306/su-rams-db"


# Super Admin: admin@sharda.edu
# Dean (School of Business and Technology): dean.sbt@sharda.edu
# Coordinator (School of Business and Technology): coord.sbt@sharda.edu
# Coordinator (School of Health Sciences): coord.shs@sharda.edu
# HOD (Computer Science & Engineering): hod.cse@sharda.edu
# RDC Admin: rdc@sharda.edu
# Supervisor: supervisor@sharda.edu
# Student: student1@sharda.edu
# You can use these email addresses along with the password admin123 to log into the different roles.

